// every match consists of rounds
// this class will define a round.
// Each round starts with a puck being added to the game, then player interaction, until a player scores. 
class Round{
	public:
		Round();
		~Round();
	private:
};
